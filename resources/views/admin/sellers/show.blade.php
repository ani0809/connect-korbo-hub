@extends('admin.layouts.app')
@section('title','Seller Overview')
@section('content')
<div class="grid grid-cols-12 gap-4"><div class="col-span-8 space-y-4"><div class="grid grid-cols-4 gap-3"><div class="bg-white border rounded-xl p-3">Sales<br><strong>{{ currency_format($stats['total_sales']) }}</strong></div><div class="bg-white border rounded-xl p-3">Orders<br><strong>{{ $stats['total_orders'] }}</strong></div><div class="bg-white border rounded-xl p-3">Balance<br><strong>{{ currency_format($stats['balance']) }}</strong></div><div class="bg-white border rounded-xl p-3">Rating<br><strong>{{ number_format($stats['avg_rating'],1) }}</strong></div></div><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold mb-2">Recent Products</h3>@foreach($seller->products as $p)<div class="text-sm border-b py-2">{{ $p->name }}</div>@endforeach</div><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold mb-2">Recent Payouts</h3>@foreach($seller->payouts as $p)<div class="text-sm border-b py-2">{{ $p->created_at?->format('Y-m-d') }} - {{ currency_format((float)$p->amount) }} - {{ ucfirst($p->status) }}</div>@endforeach</div></div><div class="col-span-4 space-y-4"><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold">Shop Info</h3><div class="text-sm mt-2">{{ $seller->shop_name }}</div><div class="text-sm">{{ $seller->shop_email }}</div><div class="text-sm">{{ $seller->shop_phone }}</div><div class="text-sm">{{ $seller->shop_address }}</div></div><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold">Quick Actions</h3><div class="mt-2 space-y-2"><a class="block btn-secondary text-center" href="{{ route('admin.sellers.approve',$seller->id) }}">Approve</a><a class="block btn-secondary text-center" href="{{ route('admin.sellers.suspend',$seller->id) }}">Suspend</a></div></div><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold">Followers</h3><form method="POST" action="{{ route('admin.sellers.followers') }}" class="mt-2 flex gap-2">@csrf<input type="hidden" name="seller_id" value="{{ $seller->id }}"><input type="number" name="count" value="{{ $seller->followers_count }}" class="border rounded px-2 py-1 w-28"><button class="btn-primary">Update</button></form></div></div></div>
@endsection
