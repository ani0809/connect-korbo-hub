@extends('seller.layouts.app')
@section('content')
<div class="space-y-4"><div class="grid grid-cols-4 gap-4"><div class="bg-white border rounded-xl p-4"><div class="text-sm text-gray-500">Today Revenue</div><div class="text-xl font-semibold">{{ currency_format($data['today_revenue']) }}</div></div><div class="bg-white border rounded-xl p-4"><div class="text-sm text-gray-500">Today Orders</div><div class="text-xl font-semibold">{{ $data['today_orders'] }}</div></div><div class="bg-white border rounded-xl p-4"><div class="text-sm text-gray-500">Monthly Revenue</div><div class="text-xl font-semibold">{{ currency_format($data['month_revenue']) }}</div></div><div class="bg-white border rounded-xl p-4"><div class="text-sm text-gray-500">Current Balance</div><div class="text-xl font-semibold">{{ currency_format($data['current_balance']) }}</div><a href="{{ route('seller.withdrawal.index') }}" class="text-sm text-teal-700">Withdraw</a></div></div><div class="grid grid-cols-2 gap-4"><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold mb-2">Recent Orders</h3><div class="space-y-2">@foreach($data['recent_orders'] as $order)<div class="text-sm border rounded p-2"><div>{{ $order->order_number }}</div><div class="text-gray-500">{{ $order->user?->name ?: $order->guest_name }}</div></div>@endforeach</div></div><div class="bg-white border rounded-xl p-4"><h3 class="font-semibold mb-2">Low Stock</h3><div class="space-y-2">@foreach($data['low_stock'] as $p)<div class="text-sm border rounded p-2 flex justify-between"><span>{{ $p->name }}</span><span class="text-red-600">{{ $p->variants->sum('stock') }}</span></div>@endforeach</div></div></div></div>
@endsection
