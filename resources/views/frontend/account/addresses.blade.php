@extends('frontend.account.layouts.app')
@section('account-content')
<div class="flex justify-between items-center mb-4"><h1 class="text-2xl font-semibold">Addresses</h1><button id="open-address-modal" class="border rounded px-3 py-2">Add New Address</button></div>
<div class="grid md:grid-cols-2 gap-4">@foreach($addresses as $address)<div class="border rounded-xl p-4"><div class="font-semibold">{{ $address->name }} @if($address->is_default_shipping)<span class="text-xs text-green-600">Default Shipping</span>@endif</div><div class="text-sm text-gray-600">{{ $address->address_line1 }}, {{ $address->city }}, {{ $address->country }}</div><div class="mt-3 flex gap-2 text-sm"><form method="POST" action="{{ route('account.addresses.default-shipping',$address->id) }}">@csrf<button class="border rounded px-2 py-1">Default Shipping</button></form><form method="POST" action="{{ route('account.addresses.default-billing',$address->id) }}">@csrf<button class="border rounded px-2 py-1">Default Billing</button></form><form method="POST" action="{{ route('account.addresses.destroy',$address->id) }}">@csrf @method('DELETE')<button class="border rounded px-2 py-1 text-red-600">Delete</button></form></div></div>@endforeach</div>
<div id="address-modal" class="fixed inset-0 bg-black/40 hidden items-center justify-center"><div class="bg-white rounded-xl p-4 w-full max-w-lg"><h3 class="font-semibold mb-2">Add Address</h3><form method="POST" action="{{ route('account.addresses.store') }}" class="grid grid-cols-2 gap-2">@csrf<input name="name" class="border rounded p-2 col-span-2" placeholder="Full name"><input name="phone" class="border rounded p-2" placeholder="Phone"><input name="email" class="border rounded p-2" placeholder="Email"><input name="address_line1" class="border rounded p-2 col-span-2" placeholder="Address line 1"><input name="city" class="border rounded p-2" placeholder="City"><input name="country" class="border rounded p-2" placeholder="Country"><button class="border rounded px-3 py-2 col-span-2">Save Address</button></form><button id="close-address-modal" class="mt-2 text-sm">Close</button></div></div>
@endsection
