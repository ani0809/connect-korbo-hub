@extends('frontend.account.layouts.app')
@section('account-content')
<div class="flex flex-wrap justify-between items-center gap-3 mb-4"><h1 class="text-2xl font-heading font-semibold text-[hsl(var(--foreground))]">My Wishlist ({{ $wishlist->total() }} items)</h1><form method="POST" action="{{ route('account.wishlist.move-all-to-cart') }}">@csrf<button class="btn-secondary">Move All to Cart</button></form></div>
<div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4">@forelse($wishlist as $item)<div class="card p-3"><img src="{{ $item->product->thumbnail_url ?? '' }}" class="w-full aspect-[4/3] object-cover rounded-lg"><div class="font-medium mt-2 text-[hsl(var(--foreground))]">{{ $item->product->name }}</div><div class="text-sm text-[hsl(var(--muted-foreground))]">{{ currency_format((float)($item->product->main_price ?? $item->product->price ?? 0)) }}</div><div class="mt-3 flex gap-2"><button class="add-to-cart-btn btn-secondary text-xs px-2.5 py-1.5" data-product-id="{{ $item->product_id }}">Add to Cart</button><button class="wishlist-btn active btn-secondary text-xs px-2.5 py-1.5 border-[hsl(var(--destructive))] text-[hsl(var(--destructive))]" data-product-id="{{ $item->product_id }}">Remove</button></div></div>@empty<div class="col-span-full"><div class="empty-state">Your wishlist is empty.</div></div>@endforelse</div>
<div class="mt-4">{{ $wishlist->links() }}</div>
@endsection
