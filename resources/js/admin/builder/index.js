import { getExtendedBlocks } from './extended-blocks';
import { registerDynamicComponents } from './dynamic-components';

window.BuilderWidgets = {
  getExtendedBlocks,
  registerDynamicComponents,
};

export { getExtendedBlocks, registerDynamicComponents };
